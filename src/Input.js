// needs to hold form state
  // 1) inputs for:
    // a) player1 name
    // b) player2 name (if there is one)
    // c) score to win
  // 2) buttons to submit each input
  // 3) handler for each button